---
import KpiValue from "../kpi-value"

// const stats = [
//   { label: "Settlement value processed", target: 52, decimals: 0, prefix: "₹", suffix: "Cr+" },
//   { label: "Sellers onboarded", target: 2000, decimals: 0, prefix: "", suffix: "+" },
//   { label: "Auto-match rate", target: 98.6, decimals: 1, prefix: "", suffix: "%" },
//   { label: "Orders reconciled / month", target: 10, decimals: 0, prefix: "", suffix: "L+" },
// ] as const
---

<section class="py-14 md:py-16" aria-label="DeepEcom by the numbers">
  <div class="mx-auto grid max-w-6xl grid-cols-2 gap-y-10 px-6 lg:grid-cols-4">
    {
      // stats.map((s) => (
      //   <div class="reveal text-center">
      //     <div class="flex items-baseline justify-center gap-1">
      //       {s.prefix && <span class="text-2xl font-extrabold tracking-tight text-primary md:text-3xl">{s.prefix}</span>}
      //       <KpiValue target={s.target} decimals={s.decimals} client:visible />
      //       <span class="text-2xl font-extrabold tracking-tight text-primary md:text-3xl">{s.suffix}</span>
      //     </div>
      //     <p class="mt-2 text-sm text-muted-foreground">{s.label}</p>
      //   </div>
      // ))
    }
  </div>
</section>
